#cloud-config
# cloud-init.fleet-host.yaml — first-boot provisioning for the squadra fleet-host VM
# (ADR-0002 §11). Lays down a NATIVE Docker daemon (no DooD/DinD), the Azure CLI
# (for `az login --identity` + the tick-time Key Vault reads + `az devops configure`),
# git, and a Python 3.11 venv, then invokes the idempotent fleet-host-activate.sh to
# install squadra from PyPI + the systemd units. The activate step is credential-free.
#
# IMPORTANT — the exact first line MUST be `#cloud-config` (no leading blank line,
# no BOM). When the fleet-host was hand-provisioned, runcmd never fired
# because the --custom-data first line was not exactly `#cloud-config`.
# This file closes that gap and three others
# found at that time: Python 3.11 (jammy ships 3.10), chown AFTER venv creation,
# and installing the systemd timer.
#
# Docker is installed from Docker's official apt repo with a pinned signed GPG key
# (not `get.docker.com | sh`): this host is the fleet's trust anchor (it holds the
# PAT and the Docker socket), so no unpinned curl-pipe-to-shell on it.
#
# The activate step installs squadra from public PyPI and needs no Key Vault grant.
# fleet-host-activate.sh is IDEMPOTENT and re-runnable — re-run it (or let the next
# boot run it) any time, e.g. to bump the pinned version. The Key Vault `get` grant
# (anthropic-api-key + fleet-ado-pat) is only needed at TICK time, not at activation.
# app-backend is NOT cloned here: `squadra fleet-tick` clones FLEET_HOME on the first
# tick (the host self-heals on the first tick).

package_update: true
package_upgrade: false

packages:
  - ca-certificates
  - curl
  - git
  - python3-venv
  - apt-transport-https
  - gnupg
  - lsb-release
  - software-properties-common   # provides add-apt-repository (deadsnakes PPA below)

write_files:
  # The squadra PyPI version fleet-host-activate.sh pins (bump at the F5 cutover
  # / fleet activation — see docs/fleet-host/SMOKE.md and the consuming repo's runtime pin).
  - path: /opt/squadra/PINNED_VERSION
    permissions: "0644"
    content: |
      0.1.0a1

  # Static fleet-host parameters consumed by fleet-host-activate.sh + the units.
  - path: /opt/squadra/fleet-host.env
    permissions: "0644"
    content: |
      FLEET_KEY_VAULT=fleet-kv
      FLEET_HOME=/opt/squadra/app-backend
      FLEET_APP_REPO_URL=https://dev.azure.com/your-org/example-project/_git/app-backend
      FLEET_PARENT_SCOPE_IDS=139
      FLEET_VENV=/opt/squadra/venv
      FLEET_USER=azureuser
      FLEET_ADO_ORG=https://dev.azure.com/your-org
      FLEET_ADO_PROJECT=example-project

  # The idempotent activation script. CANONICAL SOURCE:
  # docs/fleet-host/fleet-host-activate.sh — keep this embedded copy in sync.
  - path: /opt/squadra/fleet-host-activate.sh
    permissions: "0755"
    owner: azureuser:azureuser
    content: |
      #!/usr/bin/env bash
      # fleet-host-activate.sh — idempotent fleet-host activation (ADR-0002 §11).
      # Installs squadra from PyPI at the pinned version + renders/installs the
      # systemd units. CREDENTIAL-FREE: reads nothing from Key Vault (the ADO PAT +
      # ANTHROPIC_API_KEY are fetched at tick time by squadra.secrets). Safe to re-run.
      # Does NOT enable the timer and does NOT clone app-backend (fleet-tick does).
      set -euo pipefail
      ENV_FILE=${FLEET_HOST_ENV_FILE:-/opt/squadra/fleet-host.env}
      PIN_FILE=${FLEET_PINNED_VERSION_FILE:-/opt/squadra/PINNED_VERSION}
      log() { echo "fleet-host-activate: $*"; }
      # shellcheck disable=SC1090
      [ -f "$ENV_FILE" ] && . "$ENV_FILE"
      : "${FLEET_KEY_VAULT:?set FLEET_KEY_VAULT in $ENV_FILE}"
      : "${FLEET_HOME:?set FLEET_HOME in $ENV_FILE}"
      VENV=${FLEET_VENV:-/opt/squadra/venv}
      # az devops org/project the board adapter reads (board.py::_configured_default);
      # overridable via the env file, default your-org / example-project.
      ADO_ORG=${FLEET_ADO_ORG:-https://dev.azure.com/your-org}
      ADO_PROJECT=${FLEET_ADO_PROJECT:-example-project}
      SQUADRA_VERSION=${SQUADRA_VERSION:-$(tr -d '[:space:]' < "$PIN_FILE" 2>/dev/null || true)}
      : "${SQUADRA_VERSION:?set SQUADRA_VERSION in $ENV_FILE or write $PIN_FILE}"
      # squadra is on public PyPI (MIT, no third-party runtime deps): pip-install it
      # credential-free. The ADO PAT (fleet-ado-pat) + ANTHROPIC_API_KEY are read at
      # tick time (squadra.secrets) for app-backend + board ops — not here.
      log "installing squadra==${SQUADRA_VERSION} from PyPI into ${VENV}"
      # --force-reinstall: force a clean rebuild from PyPI on a version cutover; only
      # squadra is reinstalled (it has no third-party runtime deps).
      "$VENV/bin/pip" install --upgrade --force-reinstall "squadra==${SQUADRA_VERSION}"
      sudo "$VENV/bin/squadra" install-units \
        --key-vault "$FLEET_KEY_VAULT" --fleet-home "$FLEET_HOME" --venv-bin "$VENV/bin" \
        --app-repo-url "${FLEET_APP_REPO_URL:-}" --parent-scope-ids "${FLEET_PARENT_SCOPE_IDS:-}"
      sudo systemctl daemon-reload
      # az devops defaults the board adapter reads — set for BOTH azureuser (this
      # script) and root (the goss smoke runs as root). Stored per-user in ~/.azure.
      log "configuring az devops defaults: organization=${ADO_ORG} project=${ADO_PROJECT}"
      az devops configure --defaults "organization=${ADO_ORG}" "project=${ADO_PROJECT}"
      sudo az devops configure --defaults "organization=${ADO_ORG}" "project=${ADO_PROJECT}"
      log "done. Smoke a dry-run, then: sudo systemctl enable --now squadra.timer"

runcmd:
  # --- Docker Engine (native daemon) from the official apt repo, pinned signed key ---
  - install -m 0755 -d /etc/apt/keyrings
  - curl -fsSL https://download.docker.com/linux/ubuntu/gpg -o /etc/apt/keyrings/docker.asc
  - chmod a+r /etc/apt/keyrings/docker.asc
  - >
    echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.asc]
    https://download.docker.com/linux/ubuntu $(. /etc/os-release && echo "$VERSION_CODENAME") stable"
    > /etc/apt/sources.list.d/docker.list
  - apt-get update
  - apt-get install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin
  - systemctl enable --now docker
  - usermod -aG docker azureuser

  # --- Azure CLI (Microsoft first-party apt repo; for `az login --identity` + KV) ----
  - curl -fsSL https://aka.ms/InstallAzureCLIDeb | bash

  # --- Python 3.11 (jammy ships 3.10; squadra requires >=3.11) via deadsnakes ------
  - add-apt-repository -y ppa:deadsnakes/ppa
  - apt-get update
  - apt-get install -y python3.11 python3.11-venv

  # --- Empty squadra venv (built with 3.11), owned by azureuser so the activate ---
  # --- script's pip install needs no sudo. chown MUST follow venv creation. --------
  - python3.11 -m venv /opt/squadra/venv
  - chown -R azureuser:azureuser /opt/squadra

  # --- Install squadra from PyPI + render/install the systemd units (idempotent; ---
  # --- credential-free, safe to re-run any time). Runs as azureuser. ---------------
  - su - azureuser -c 'bash /opt/squadra/fleet-host-activate.sh' >> /var/log/fleet-host-activate.log 2>&1 || true

  # --- Provenance breadcrumb -------------------------------------------------------
  - docker --version > /opt/squadra/PROVISIONED 2>&1
  - az version >> /opt/squadra/PROVISIONED 2>&1 || true
